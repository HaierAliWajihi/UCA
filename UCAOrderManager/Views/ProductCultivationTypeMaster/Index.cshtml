@model IEnumerable<UCAOrderManager.Models.Product.ProductCultivationTypeViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductCultivationTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCultivationTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCultivationTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCultivationTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCultivationTypeID })
        </td>
    </tr>
}

</table>
