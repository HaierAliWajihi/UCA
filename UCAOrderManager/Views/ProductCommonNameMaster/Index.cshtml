@model IEnumerable<UCAOrderManager.Models.Product.ProductCommonNameViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductCommonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCommonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductCommonNameID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductCommonNameID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductCommonNameID })
        </td>
    </tr>
}

</table>
