@model UCAOrderManager.Models.Product.ProductCommonNameViewModel

@{
    ViewBag.Title = "Create";
}

<h2>@(Model == null || Model.ProductCommonNameID == 0? "Create" : "Edit")</h2>

@using (Html.BeginForm("Create", "ProductCommonNameMaster")) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Product Common Name</h4>
        <hr />
        @Html.ValidationSummary(true)

        @if (Model != null && Model.ProductCommonNameID != 0)
        {
            @Html.HiddenFor(r=> r.ProductCommonNameID)
        }
        
        <div class="form-group">
            @Html.LabelFor(model => model.ProductCommonName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.ProductCommonName, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ProductCommonName)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>
